# sophia

going to remote

```git
git branch -a
```

```git
git checkout -b <name>
```

```git
git add . && git commit -m "pushing to remote" && git push <remote> <name>
```
- where remote = origin
- where name = example

- Check for branches

```git
git branch -a
```

